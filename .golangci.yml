version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - revive
  settings:
    revive:
      rules:
        - name: exported
        - name: package-comments
        - name: comment-spacings
